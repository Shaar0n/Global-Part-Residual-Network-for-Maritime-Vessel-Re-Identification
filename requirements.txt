# Core training/eval stack
torch>=2.7.1
torchvision>=0.22.1
# For CUDA builds, install from the PyTorch CUDA index instead, e.g.:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
pandas>=2.3
numpy>=2.2
pillow>=12.0
tqdm>=4.67
matplotlib>=3.10

# Used by app.py (interactive retrieval demo)
streamlit>=1.59

# Used by demo.py (VesselReID.json dataset download script)
requests
retrying
